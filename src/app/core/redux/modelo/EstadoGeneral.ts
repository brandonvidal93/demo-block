import { EstadoProducto } from './EstadoProducto';
// import { Estado{{ > comandoPascal}} } from './Estado{{ > comandoPascal}}';

export interface EstadoGeneral {
  productos: EstadoProducto;
  // {{comando}}s: Estado{{ > comandoPascal}};
}
